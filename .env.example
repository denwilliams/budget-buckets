# Database Configuration
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Local Development (Docker Compose)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/budget_buckets

# Neon Serverless Postgres (Production/Vercel)
# Use the pooled connection string for serverless environments
# Get these from: https://console.neon.tech/ > Your Project > Connection Details
# Important: Use the "Pooled connection" string (ends with -pooler.region.neon.tech)
# Example format:
# DATABASE_URL=postgres://username:password@ep-xxxxx-pooler.us-east-1.neon.tech/dbname?sslmode=require

# Direct connection (for migrations and Prisma Studio only)
# Use this locally when running migrations: npx prisma migrate dev
# Example format:
# DIRECT_URL=postgres://username:password@ep-xxxxx.us-east-1.neon.tech/dbname?sslmode=require

# Node Environment
# Options: development, production, test
NODE_ENV=development

# Next.js Configuration (optional, defaults shown)
# Port for the Next.js dev server (default: 3000)
# PORT=3000

# Application Configuration (optional)
# Base URL for the application (useful for production deployments)
# NEXT_PUBLIC_APP_URL=http://localhost:3000
